/**
 * Chat Mentions Module - Enhancement 33
 * Allows users to mention others in chat (@username)
 */
(function() {
    'use strict';
    function mentionUser(username) {
        // ...simulate mention
        alert('Mentioned @' + username);
    }
    window.ChatMentions = { mentionUser };
})();